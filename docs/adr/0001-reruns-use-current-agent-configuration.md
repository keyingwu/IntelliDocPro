# Reruns use the current Document Agent configuration

During the MVP, a Run is immutable and running it again creates a new Run using the Document Agent's current configuration. Exact replay with the earlier configuration is intentionally unsupported, trading reproducibility for a simpler user experience and data model.
